# Backend env for Khansa Collection
# Copy to .env then adjust.

# MySQL / MariaDB (XAMPP)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=khansa_collection

# JWT
JWT_SECRET=change-me-super-secret
JWT_EXPIRES_DAYS=7

# CORS
# Comma-separated list. Use * for dev.
CORS_ORIGINS=*
